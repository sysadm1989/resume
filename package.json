{
  "name": "resume",
  "version": "1.0.0",
  "private": true,
  "description": "Senior DevOps resume (Markdown → HTML) + OpenCode vacancy match",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node --env-file=.env server.mjs",
    "dev": "node --watch --env-file=.env server.mjs",
    "health": "curl -fsS http://127.0.0.1:${PORT:-8787}/api/health | python3 -m json.tool"
  },
  "dependencies": {
    "express": "^5.1.0",
    "multer": "^2.0.2",
    "pdf-parse": "^2.4.5"
  }
}
